name: Release Artifacts

# Builds and publishes everything attached to a GitHub release: the Rapport
# Agent installer for macOS/Windows/Linux, the Rapport Installer (backend/
# frontend bootstrap) for the same three platforms, and the backend/frontend
# Docker images themselves (pushed to GHCR so the installer can `docker pull`
# prebuilt images instead of building Chromium locally). Deliberately
# decoupled from ci.yml's push-triggered jobs — this only runs when a
# release is published (or manually via workflow_dispatch), not on every
# push to main.

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to attach installers to (e.g. v4.3.5)"
        required: true

permissions:
  contents: write
  packages: write

jobs:
  publish-images:
    name: Publish backend/frontend images to GHCR
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Resolve version
        id: version
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Chromium install is the slow part (~20 apt packages + playwright
      # install chromium) and rarely changes — GHA cache means a release
      # with no Dockerfile.playwright-base/requirements.txt changes reuses
      # the cached layers instead of reinstalling Chromium from scratch.
      - name: Build + push playwright base image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile.playwright-base
          platforms: linux/amd64
          push: true
          tags: ghcr.io/egulinsky/rapport-playwright-base:latest
          cache-from: type=gha,scope=playwright-base
          cache-to: type=gha,mode=max,scope=playwright-base

      # backend/Dockerfile's `FROM rapport-playwright-base:latest` is a bare
      # local tag (unchanged from the existing self-hosted deploy flow,
      # which builds it locally right before building backend — never
      # pushed anywhere there). docker/setup-buildx-action's `docker-
      # container` builder runs in its own isolated BuildKit instance with
      # no access to the runner's local Docker image store, so a bare FROM
      # tag resolves as a *pull* from docker.io/library/... by default and
      # fails ("pull access denied") — confirmed the hard way on the first
      # two release runs (the first also tried to *push* the bare tag
      # there for the same reason). build-contexts redirects that FROM
      # reference straight to the image just pushed above, without
      # depending on any local Docker daemon state at all.
      - name: Build + push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          platforms: linux/amd64
          push: true
          build-contexts: |
            rapport-playwright-base:latest=docker-image://ghcr.io/egulinsky/rapport-playwright-base:latest
          tags: |
            ghcr.io/egulinsky/rapport-backend:${{ steps.version.outputs.version }}
            ghcr.io/egulinsky/rapport-backend:latest
          cache-from: type=gha,scope=backend
          cache-to: type=gha,mode=max,scope=backend

      - name: Build + push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          platforms: linux/amd64
          push: true
          build-args: |
            BUILD_NUMBER=${{ github.run_number }}
          tags: |
            ghcr.io/egulinsky/rapport-frontend:${{ steps.version.outputs.version }}
            ghcr.io/egulinsky/rapport-frontend:latest
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,mode=max,scope=frontend

      # GHCR packages pushed via GITHUB_TOKEN default to private regardless
      # of repo visibility — without this, an end user's `docker pull` on
      # the installer's bundled compose file fails with "unauthorized".
      # Idempotent, safe to run on every release.
      - name: Make packages public
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          for pkg in rapport-backend rapport-frontend rapport-playwright-base; do
            gh api --method PATCH "/user/packages/container/${pkg}" -f visibility=public \
              || echo "::warning::Could not set ${pkg} to public — check package visibility manually in GHCR settings."
          done

  macos:
    name: Build macOS .dmg
    runs-on: macos-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Resolve version
        id: version
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Install packaging dependencies
        working-directory: agent
        run: |
          python3 -m pip install --upgrade pip
          pip install -r packaging/requirements-packaging.txt

      - name: Build .dmg
        working-directory: agent
        run: packaging/build_dmg.sh "${{ steps.version.outputs.version }}"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ steps.version.outputs.tag }}" \
            "agent/packaging/dist/Rapport-Agent-${{ steps.version.outputs.version }}.dmg" \
            --clobber

  windows:
    name: Build Windows .zip
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Resolve version
        id: version
        shell: bash
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Install packaging dependencies
        working-directory: agent
        run: |
          python -m pip install --upgrade pip
          pip install -r packaging\requirements-packaging-windows.txt

      - name: Build .zip
        working-directory: agent
        run: packaging\build_windows.ps1 -Version "${{ steps.version.outputs.version }}"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          gh release upload "${{ steps.version.outputs.tag }}" \
            "agent/packaging/dist/Rapport-Agent-${{ steps.version.outputs.version }}-windows.zip" \
            --clobber

  linux:
    name: Build Linux .tar.gz
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Resolve version
        id: version
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Install packaging dependencies
        working-directory: agent
        run: |
          python3 -m pip install --upgrade pip
          pip install -r packaging/requirements-packaging-linux.txt

      - name: Build .tar.gz
        working-directory: agent
        run: bash packaging/build_linux.sh "${{ steps.version.outputs.version }}"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ steps.version.outputs.tag }}" \
            "agent/packaging/dist/rapport-agent-${{ steps.version.outputs.version }}-linux.tar.gz" \
            --clobber

  installer-macos:
    name: Build Rapport Installer macOS .dmg
    runs-on: macos-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Resolve version
        id: version
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Install packaging dependencies
        working-directory: installer
        run: |
          python3 -m pip install --upgrade pip
          pip install -r packaging/requirements-packaging.txt

      - name: Build .dmg
        working-directory: installer
        run: packaging/build_dmg.sh "${{ steps.version.outputs.version }}"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ steps.version.outputs.tag }}" \
            "installer/packaging/dist/Rapport-Installer-${{ steps.version.outputs.version }}.dmg" \
            --clobber

  installer-windows:
    name: Build Rapport Installer Windows Setup.msi
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      # A real Windows Installer package (a single MSI, WixUI_InstallDir's own
      # license/directory/progress/finish wizard) instead of the previous
      # PyInstaller+NSIS approach -- no Python involved. WiX itself is pulled
      # in via NuGet PackageReferences in RapportPackage.wixproj during
      # `dotnet build`, so no separate CLI tool install step is needed here
      # (unlike NSIS's choco install above). windows-latest already has a
      # .NET SDK preinstalled.
      - name: Resolve version
        id: version
        shell: bash
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Build Setup.msi
        working-directory: installer/packaging/windows-wix
        shell: pwsh
        run: ./build_windows_wix.ps1 -Version "${{ steps.version.outputs.version }}"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          gh release upload "${{ steps.version.outputs.tag }}" \
            "installer/packaging/windows-wix/dist/Rapport-Setup-${{ steps.version.outputs.version }}.msi" \
            --clobber

  installer-linux:
    name: Build Rapport Installer Linux .tar.gz
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Resolve version
        id: version
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=${TAG#v}" >> "$GITHUB_OUTPUT"

      - name: Install packaging dependencies
        working-directory: installer
        run: |
          python3 -m pip install --upgrade pip
          pip install -r packaging/requirements-packaging-linux.txt

      - name: Build .tar.gz
        working-directory: installer
        run: bash packaging/build_linux.sh "${{ steps.version.outputs.version }}"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ steps.version.outputs.tag }}" \
            "installer/packaging/dist/rapport-installer-${{ steps.version.outputs.version }}-linux.tar.gz" \
            --clobber
